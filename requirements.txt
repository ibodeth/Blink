# ---------------------------------------------------------------------------
# Blink runtime dependencies (pinned for reproducible builds).
# ---------------------------------------------------------------------------

# Web framework / server
fastapi==0.115.6
uvicorn[standard]==0.34.0
websockets==14.1

# Configuration & validation
pydantic==2.10.4
pydantic-settings==2.7.1

# HTTP client
requests==2.32.3

# System telemetry / process control
psutil==6.1.1

# Audio I/O and DSP
sounddevice==0.5.1
numpy==2.2.1

# Speech-to-text
faster-whisper==1.1.0

# Wake word detection
openwakeword==0.6.0

# Text-to-speech (primary: Kokoro ONNX, fallback: pyttsx3 / SAPI5)
kokoro-onnx==0.4.4
pyttsx3==2.98

# Music playback / streaming
yt-dlp==2024.12.13

# Desktop window (desktop mode only; not required in headless/Docker)
pywebview==5.3.2

# ---------------------------------------------------------------------------
# Test-only dependencies live in requirements-dev.txt
# ---------------------------------------------------------------------------
